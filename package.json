{
  "name": "proxy-gateway",
  "private": true,
  "version": "1.1.0",
  "description": "PROXY d.o.o. — Gateway landing page (proxy1.hr) — React + Vite frontend, Node/Express server",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server/index.js",
    "serve": "npm run build && npm start",
    "optimize:images": "node scripts/optimize-images.mjs"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^4.22.1",
    "helmet": "^8.1.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "sharp": "^0.34.5",
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
